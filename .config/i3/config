exec_always curl -d "i3 reload/startup" https://ntfy.sh/nNL0Wv5dh7

exec xrandr --output HDMI-A-0 --primary

# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!


default_border pixel 3
default_floating_border pixel 4
for_window [class="VSCodium"] border pixel 3
for_window [class="Obsidian"] border pixel 3
for_window [class="Thunar"] border pixel 3
for_window [class="Alacritty"] border pixel 3


set $mod Mod4

exec_always /home/mostypc123/set-sreens.sh
exec picom
exec dunst

# Font for window titles and bar
font pango:Fira Code Nerd Font 11

# Gnome Terminal
for_window [class="Alacritty"] title_format " %title"
for_window [class="gnome-terminal"] title_format " %title" 
# Firefox
for_window [class="firefox"] title_format " Firefox"
# Thunar
for_window [class="thunar"] title_format " File Manager"
# Steam
for_window [class="Steam"] title_format " %title"
# feh
for_window [class="feh"] title_format " Image Viewer"
# GitHub in Firefox
for_window [class="Firefox" title="*GitHub*"] title_format " GitHub - %title"

# Gaps
gaps inner 30

# Autostart
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
exec --no-startup-id nm-applet

# Volume control
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Floating modifier
floating_modifier $mod
tiling_drag modifier titlebar

# Applications
bindsym $mod+Return exec alacritty
bindsym $mod+d exec rofi -show drun
bindsym $mod+Shift+w exec rofi -show window
bindsym $mod+Shift+d exec rofi -show run 
bindsym $mod+Shift+q kill

# Focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+ocircumflex focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move windows
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+ocircumflex move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Layouts
bindsym $mod+h split h
bindsym $mod+v split v
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
bindsym $mod+a focus parent

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+plus workspace number $ws1
bindsym $mod+lcaron workspace number $ws2
bindsym $mod+scaron workspace number $ws3
bindsym $mod+ccaron workspace number $ws4
bindsym $mod+tcaron workspace number $ws5
bindsym $mod+zcaron workspace number $ws6
bindsym $mod+yacute workspace number $ws7
bindsym $mod+aacute workspace number $ws8
bindsym $mod+iacute workspace number $ws9
bindsym $mod+eacute workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+lcaron move container to workspace number $ws2
bindsym $mod+Shift+scaron move container to workspace number $ws3
bindsym $mod+Shift+ccaron move container to workspace number $ws4
bindsym $mod+Shift+tcaron move container to workspace number $ws5
bindsym $mod+Shift+zcaron move container to workspace number $ws6
bindsym $mod+Shift+yacute move container to workspace number $ws7
bindsym $mod+Shift+aacute move container to workspace number $ws8
bindsym $mod+Shift+iacute move container to workspace number $ws9
bindsym $mod+Shift+eacute move container to workspace number $ws10

# Reload / Restart / Exit
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Confirm exit' -B 'Confirm' 'i3-msg exit'"

# Resize mode
mode "resize" {
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym ocircumflex resize grow width 10 px or 10 ppt
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# Everforest Colors
# -----------------
set $bg        #2d353b
set $bg2       #343f44
set $fg        #d3c6aa
set $fg_dim    #9da9a0
set $green     #a7c080
set $aqua      #83c092
set $blue      #7fbbb3
set $orange    #e69875
set $red       #e67e80
set $yellow    #dbbc7f

# Client colors
# class                 border    background text     indicator child_border
client.focused          #66fff7   $bg2       $fg      $blue     #66fff7
client.unfocused        $blue      $bg        $fg_dim  $bg2      $blue
client.focused_inactive $bg2      $bg        $fg_dim  $bg2      $bg2
client.urgent           $red      $bg        $fg      $red      $red
client.placeholder      $bg       $bg        $fg_dim  $bg       $bg

bar {
    status_command i3status
    position top
    font pango:Fira Code Nerd Font 14

    colors {
        background #000000
        statusline #ffffff
        separator  #888888

        focused_workspace  #ffffff  #ffffff  #000000
        active_workspace   #444444  #ffffff  #ffffff
        inactive_workspace #000000  #000000  #888888
        urgent_workspace   #ff0000  #000000  #ffffff
    }
}


# Screenshots
bindsym Print exec scrot ~/Pictures/screenshot-%Y-%m-%d-%H%M%S.png
bindsym Shift+Print exec scrot -s ~/Pictures/screenshot-%Y-%m-%d-%H%M%S.png
bindsym Ctrl+Print exec scrot -u ~/Pictures/screenshot-%Y-%m-%d-%H%M%S.png
